PORT=3001
DB_PATH=./data/process-twin.sqlite
# Required for GET /api/orders/:id/explanation. Without it, explanations
# always fall back to the deterministic template (confidence: "low").
ANTHROPIC_API_KEY=
